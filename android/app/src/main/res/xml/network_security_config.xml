<?xml version="1.0" encoding="utf-8"?>
<!--
  Local dev only: allows plaintext HTTP to the civic_voice_api dev backend
  before it has TLS. Scoped to specific dev hosts, not the whole app —
  everything else still requires HTTPS. Remove once the backend is
  deployed behind HTTPS.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">192.168.100.8</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
